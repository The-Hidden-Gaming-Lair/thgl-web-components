#!/usr/bin/env sh
# CI-parity gate: block the push if typecheck, lint or unit tests fail (the errors that
# otherwise only surface as a red GitHub Actions build). Bypass in a pinch with
# `git push --no-verify`.
bun run verify
